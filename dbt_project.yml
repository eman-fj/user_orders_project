name: 'user_orders_project'
version: '1.0.0'
config-version: 2

profile: 'user_orders_project'

model-paths: ["models"]
seed-paths: ["seeds"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
snapshot-paths: ["snapshots"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target"]

models:
  user_orders_project:
    staging:
      +materialized: view
    marts:
      +materialized: table